import React from "react";
import notes from '../notes'

function createNote(data) {
  <Note
    key={data.key};
  

}

function Note() {
  return (
    <div className="note">
      <h1>This is the no